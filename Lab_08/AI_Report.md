# AI_REPORT lab_8.1

## 1. Информация о использовании AI
- Инструмент: Copilot / GitHub, Gemini / Google AI Studio
- Дата: 16.03.2026

## 2. Конкретные промпты

### Промпт 1:
"Как правильно использовать getStaticProps и revalidate в Next.js Pages Router"

Что получил: Пошаговое объяснение SSG и ISR, когда ставить `revalidate`, и как обновляются данные без полной пересборки.

### Промпт 2:
"Покажи пример динамического маршрута posts/[id] через getStaticPaths"

Что получил: Пример генерации списка путей, использование `fallback: \"blocking\"` и обработка кейса с отсутствующим постом.

### Промпт 3:
"Как типизировать getStaticProps и getStaticPaths в TypeScript"

Что получил: Пример использования `GetStaticProps`, `GetStaticPaths` и интерфейсов для параметров маршрута.

### Промпт 4:
"У меня ошибки в коде, помоги их найти и решить"

Что получил: Подсказки по проверке `notFound`, типам `params` и связям между post/author.

## 3. Модификация и проверка
- Реализованы статические страницы с `getStaticProps`
- Добавлены динамические маршруты `posts/[id]`
- Настроен ISR (`revalidate: 60`) на главной и страницах постов
- Добавлена проверка отсутствующих данных через `notFound: true`

## 4. Что узнал
1. Разница между SSG и SSR в Next.js Pages Router
2. Как работает `getStaticPaths` для динамических страниц
3. Для чего нужен `fallback: "blocking"`
4. Как использовать ISR для обновления статического контента
5. Как типизировать props и route params в Next.js + TypeScript

## 5. Процент использования
10 - 15% (AI помог с архитектурой рендеринга и типизацией)

# AI_REPORT lab_8.2

## 1. Информация о использовании AI
- Инструмент: Copilot / GitHub, Gemini / Google AI Studio
- Дата: 16.03.2026

## 2. Конкретные промпты

### Промпт 1:
"Когда использовать getServerSideProps вместо getStaticProps"

Что получил: Критерии выбора SSR/SSG для персонализированных и часто обновляемых данных.

### Промпт 2:
"Сделай пример dashboard с SSR и несколькими источниками данных"

Что получил: Шаблон загрузки пользователя, уведомлений и аналитики через `Promise.all` в `getServerSideProps`.

### Промпт 3:
"Как корректно обработать ошибку в SSR странице"

Что получил: Рекомендацию возвращать безопасные fallback-данные и сообщение об ошибке в props.

### Промпт 4:
"У меня ошибки в коде, помоги их найти и решить"

Что получил: Подсказки по типам данных dashboard и проверке отображения на страницах `about` и `about-ssr`.

## 3. Модификация и проверка
- Реализован `dashboard.tsx` на SSR (`getServerSideProps`)
- Добавлены сервисы в `lib/api.ts` для dashboard-данных
- Реализовано сравнение SSR и SSG на страницах `about-ssr` и `about`
- Настроен смешанный подход: SSR для динамики, SSG/ISR для статичного контента

## 4. Что узнал
1. Как организовать SSR-страницы для персонализированных данных
2. Как сравнивать поведение SSG и SSR на одном проекте
3. Как делать безопасный fallback при ошибке загрузки в SSR
4. Почему смешанная стратегия рендеринга часто оптимальна

## 5. Процент использования
10 - 15% (AI помог с выбором стратегии рендеринга и доработкой SSR-логики)
